% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ctwas_wrapper.R
\name{update_weights_gwas}
\alias{update_weights_gwas}
\title{Function to perform allele flip QC and harmonization on the weights and GWAS against LD for a region. 
FIXME: GWAS loading function from Haochen for both tabix & column-mapping yml application 
Main Steps: 
1. allele QC for TWAS weights against the LD meta
2. allele QC for GWA Ssummary stats against the LD meta
3. adjust susie/mvsusie weights based on the overlap variants}
\usage{
update_weights_gwas(
  refined_twas_weights_data,
  gwas_file,
  ld_meta_file_path,
  scale_weights = FALSE
)
}
\arguments{
\item{refined_twas_weights_data}{List of list of twas weights output from twas pipeline with following list items.}

\item{gwas_file}{A file path for a dataframe of GWAS summary statistics with column name "chrom" (or #chrom" if tabix-indexed),
"pos", "A2", "A1".}

\item{ld_meta_file_path}{A tab-delimited data frame with colname "#chrom", "start", "end", "path", where "path" column
contains file paths for both LD matrix and bim file and is separated by ",". Bim file input would expect no headers, while the
columns are aligned in the order of "chrom", "variants", "GD", "pos", "A1", "A2", "variance", "allele_freq", "n_nomiss".}

\item{scale_weights}{TRUE/FALSE statement. If turn off, the post-qc/harmonized weights will not be scaled by SNP variance from LD.}
}
\value{
A list of list for harmonized weights and dataframe of gwas summary statistics that is add to the original input of 
refined_twas_weights_data under each context.
}
\description{
Function to perform allele flip QC and harmonization on the weights and GWAS against LD for a region. 
FIXME: GWAS loading function from Haochen for both tabix & column-mapping yml application 
Main Steps: 
1. allele QC for TWAS weights against the LD meta
2. allele QC for GWA Ssummary stats against the LD meta
3. adjust susie/mvsusie weights based on the overlap variants
}
